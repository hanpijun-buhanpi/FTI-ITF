将文件的数据读取出来保存成byte数组
将byte数组转成int数组
将int数组输入图片数据

图片数据：一个像素3个字节，r，g，b，各一个
保留前9个像素，共27个字节，
18个字节用来存储后缀名，由后往前的顺序进行存储，存储时由char转byte，即1个字符=2个字节，18个字节最多允许存放9个字符后缀的文件
4个字节用来存放数据的长度（int类型），即最大能存放2^31-1=2147483647字节的文件，即约2GB的文件
2个字节用来存放加密方式的序号（short类型）
3个字节用来存放特殊特殊加密用的数字（byte类型）